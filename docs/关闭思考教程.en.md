## Disable Thinking on DeepSeek v4 Series
- Set the `ModelParams` parameter to `{"thinking": {"type": "disabled"}}` to disable thinking
- Search for `[AutoLLM]` and modify it according to the example below:
```ini
[AutoLLM]
ModelParams={"thinking": {"type": "disabled"}}
```
